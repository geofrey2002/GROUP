#include <stdio.h>

int main(void) {
    int age;

    printf("elo my friends\n");
    printf("Enter your age: ");
    if (scanf("%d", &age) == 1) {
        printf("your age is: %d\n", age);
    } else {
        printf("Invalid input.\n");
    }

    return 0;
}
